{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "spago build",
    "test": "spago -x test.dhall test"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.15.0",
    "purescript-psa": "^0.7.2",
    "purs-tidy": "^0.8.0",
    "rimraf": "^2.6.1",
    "spago": "^0.20.9"
  }
}
